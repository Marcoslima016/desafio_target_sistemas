import 'package:flutter/material.dart';

class InfosStatisticsModal extends StatelessWidget {
  const InfosStatisticsModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
